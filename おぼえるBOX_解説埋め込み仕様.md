# おぼえるBOX 解説事前埋め込み 仕様書

作成日: 2026/08/11

## 1. 背景と目的

カード表面の「🔎 Geminiで調べる」は、iOSのPWAから開くとSafariのログインセッションが
引き継がれず未ログインのGeminiが開くうえ、GeminiはURLでの質問prefillを公式サポートして
いないため、手動ペーストが必要で手間が多い。

そこで **解説文をあらかじめデッキJSONに埋め込み、タップした瞬間にアプリ内で表示する**
方式に変更する。ログイン不要・通信不要・待ち時間ゼロになる。

### 決定事項

| 項目 | 決定 |
|---|---|
| 方式 | 事前埋め込み（Gemini API連携はしない） |
| 発音表記 | カタカナ ＋ IPA ＋ フォニックス の3点セット |
| 記載項目 | コアイメージ／使い方・文型／例文／間違えやすい点 |
| 対象範囲 | 今回は英語のみ（`eigo50.json` 50項目、`eiken200.json` 200項目） |
| 理社国200語 | 今回は対象外。別途ルールを決めて後日対応 |

### 容量の検証結果（実測済み・問題なし）

| 状態 | localStorage実体 |
|---|---|
| 現状の250枚 | 66.8 KB |
| 解説150字を追加 | 179.1 KB |
| 解説300字を追加 | 288.9 KB |

Safariのlocalstorage上限は1オリジンあたり最低5MB。最大でも上限の約6%で収まる。

---

## 2. データ構造

デッキJSONの各要素に以下のフィールドを追加する。**既存の `term` / `meaning` /
`reading` は変更しない。**

```json
{
  "term": "keep",
  "meaning": "保つ、〜し続ける",
  "reading": "キープ",
  "ipa": "kiːp",
  "phonics": "ee → 「イー」と長くのばす音",
  "core": "手に持ったものを手放さない。そのままの状態でずっと持ち続けるイメージ",
  "usage": "keep + 名詞（物を持ち続ける）／keep + 〜ing（動作を続ける）",
  "example": "Keep going.｜そのまま進み続けて",
  "confuse": ""
}
```

### フィールド定義

| フィールド | 必須 | 型 | 内容 |
|---|---|---|---|
| `term` | ○ | string | 既存。変更しない |
| `meaning` | ○ | string | 既存。変更しない |
| `reading` | ○ | string | 既存。カタカナ読み |
| `ipa` | △ | string | 発音記号。**スラッシュは付けず中身だけ**（表示側で `/ /` を付与） |
| `phonics` | △ | string | 綴りと音の対応ルール。1〜2個まで |
| `core` | ○ | string | コアイメージ |
| `usage` | ○ | string | 使い方・文型 |
| `example` | ○ | string | 例文。`英文｜和訳` の形式（区切りは全角パイプ `｜`） |
| `confuse` | 任意 | string | 間違えやすい点。不要なら空文字 `""` |

△ = 単語のみ必須、熟語・文法項目では空文字 `""` にする

---

## 3. 記載ルール

### 3.1 共通ルール

- 読み手は**高校受験を控えた中学生**。専門用語は使わない
- 文体は「です・ます」ではなく**言い切り（体言止め可）**でコンパクトに
- 改行は入れない（表示側で整形する）
- ダブルクォート `"` は使わない（JSONエスケープを避けるため）。必要なら「」を使う

### 3.2 `ipa`（発音記号）

- **スラッシュを含めない**。`kiːp` と書く（`/kiːp/` ではない）
- アメリカ英語を基準にする
- 第一強勢の `ˈ` は入れる。第二強勢 `ˌ` は3音節以上の語のみ
- 例: `ɪmˈpɔːrtnt` / `ˈnesəseri` / `ˌɪntrəˈduːs`

### 3.3 `phonics`（フォニックス）

- **その単語で一番つまずきやすい綴り1〜2箇所**だけを取り上げる。全部は書かない
- 書式: `綴り → 読み方の説明`
- 複数ある場合は `／` で区切る
- 例:
  - `ee → 「イー」と長くのばす音`
  - `ough → ここでは「オー」。読み方が多いので要注意`
  - `kn → k は読まない。「ン」ではなく「ナ行」の音だけ`
  - `ci → 「シ」。c が i の前にくると「ス」系の音になる`
- サイレントE、二重母音、C/Gの軟音化など**ルールとして一般化できる説明**を優先する
- 該当する特徴がない平易な綴り（例: `plan`）は空文字 `""` でよい

### 3.4 `core`（コアイメージ）

- **40〜70字程度**
- その語の中心にある1つのイメージを書く。訳語の羅列にしない
- 複数の訳語がある語は、**なぜその訳になるのかが繋がる**ように書く
- 例:
  - `keep` → 手に持ったものを手放さない。そのままの状態でずっと持ち続けるイメージ
  - `leave` → その場に置いたまま離れていく。「去る」も「置き忘れる」も同じ動き
  - `hold` → 力を入れてしっかりつかんで動かさない。keepより「今まさに握っている」感じ
- ✗ 悪い例: 「保つ、続ける、飼う、という意味の動詞」← 訳語の列挙にすぎない

### 3.5 `usage`（使い方・文型）

- **30〜60字程度**
- 入試の文法問題で問われるパターンを優先する
- 後ろに動名詞をとるか不定詞をとるか、自動詞か他動詞か、前置詞は何かを明示
- 例:
  - `enjoy + 〜ing（to不定詞は不可）。入試頻出`
  - `arrive は自動詞。arrive at（狭い場所）／arrive in（広い場所）`

### 3.6 `example`（例文）

- **英文は8語以内**。中学既習の語彙だけで作る
- 書式: `英文｜和訳`（区切りは全角パイプ `｜`）
- その語の**代表的な使い方**が分かる文にする
- 例: `I'm looking forward to seeing you.｜あなたに会うのを楽しみにしています`

### 3.7 `confuse`（間違えやすい点）

- **混同しやすいペアがある語だけ**記載。なければ空文字 `""`
- **40〜80字程度**
- 例:
  - `borrow` → `lend と混同注意。borrow は「借りる（自分がもらう側）」、lend は「貸す（相手に渡す側）」`
  - `hear` → `listen との違い。hear は自然と聞こえる、listen は意識して聞く`
- 目安として、250項目のうち**30〜50項目程度**に付けば十分

### 3.8 熟語・文法項目の扱い

`eigo50.json` の後半20項目（`be interested in 〜`、`There is / There are 〜`、
`have just 〜(過去分詞)` など）は以下のとおり。

- `ipa` = `""`、`phonics` = `""`（綴り単位のルールが適用できないため）
- `core` / `usage` / `example` は必須
- `core` は「なぜその意味になるか」を構成語から説明する
  - 例: `look forward to 〜` → 前方（forward）に視線を向けて待つイメージ。to は前置詞なので後ろは名詞か〜ing

---

## 4. UI表示仕様

カード**裏面**（意味が表示される面）に折りたたみセクションを追加する。

```
　　　　保つ、〜し続ける

　　［ 📖 くわしい解説 ▼ ］     ← タップで開閉

　🔤 読み方
　　 キープ ／ /kiːp/
　　 ee → 「イー」と長くのばす音

　💡 コアイメージ
　　 手に持ったものを手放さない。
　　 そのままの状態でずっと持ち続けるイメージ

　🔧 使い方
　　 keep + 名詞 ／ keep + 〜ing

　✏️ 例文
　　 Keep going.
　　 そのまま進み続けて

　⚠️ 注意
　　 （confuseがある場合のみ表示）
```

### 実装上の注意

- **デフォルトは閉じた状態**。カードをめくった直後に解説が見えると答えの判定に影響する
- 開閉状態はカードを切り替えるたびにリセットする
- 折りたたみの開閉クリックは `event.stopPropagation()` で**カードのフリップを止める**
  （既存の `.memo-input` と同じ扱い）
- 該当フィールドが空文字の項目は**見出しごと非表示**にする
- 解説セクションが1つも無いカードでは「📖 くわしい解説」ボタン自体を出さない
- 既存の `.memo-input`（メモ欄）は解説の**下**に配置する
- マイクラ風UIのトーンに合わせ、`--pixel-font` とドット風ボーダー（`border:2px solid #000`）を踏襲する

### 「🔎 Geminiで調べる」ボタンの扱い

- **残す**。埋め込み解説で足りないときの逃げ道として機能する
- ただしラベルを `🔎 もっと詳しく` に変更し、副次的な位置づけにする
- 未ログイン問題は解決しないため、使い方タブに「Geminiは別途ログインが必要」と注記する

---

## 5. 作業手順（次チャットで実施）

1. `decks/eigo50.json`（50項目）に5フィールドを追加
2. `decks/eiken200.json`（200項目）に5フィールドを追加
   - 分量が多いためバッチ分割を検討（50項目ずつなど）
3. `index.html` の裏面レンダリング部（`renderCard` 内の `<div class="face back">`）を改修
4. `makeCard()` に新フィールドを追加し、取込・書き出し・バックアップの各経路で引き回す
   - 対象箇所は `reading` を追加したときと同じ5箇所
5. Geminiプロンプト（`geminiPromptBox`）に新フィールドの出力指示を追加
6. 使い方タブに解説機能の説明を追加

### 品質チェック（必須）

- `node --check` でスクリプトの構文チェック
- JSONのパースと全項目のフィールド検査（必須フィールドの欠落・空文字チェック）
- `example` が `｜` を必ず1つ含むこと
- `ipa` にスラッシュ `/` が混入していないこと
- 旧カード（新フィールドを持たない）で表示が壊れないことの後方互換テスト
- 表示ロジックを空フィールドの各パターンで出力確認

---

## 6. 現在の実装状況（2026/08/11 時点）

- ブランチ `feature/minecraft-ui` が GitHub Pages の公開元（`main` は古いまま）
- カード計450枚（英50＋英検200＋理社国200）
- カタカナ読み `reading` は英語250項目すべてに付与済み
- 🔊 音声読み上げ（Web Speech API、`lang='en-US'`）実装済み
  - 英字判定 `isEnglishTerm()` により、自作カードでも英字なら🔊が出る
- デッキは `decks/manifest.json` によるマニフェスト管理

### 次チャットの始め方

最新の `index.html` と本仕様書を添付して開始する。
